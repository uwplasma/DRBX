from __future__ import annotations

"""Hermes shifted-transform mirror.

Planned in Phase 2 of `/Users/rogerio/local/jax_drb/plan.md`.
"""


def to_field_aligned_nox(*args, **kwargs):
    raise NotImplementedError("Phase 2: mirror `toFieldAligned(..., RGN_NOX)` is not landed yet.")


def from_field_aligned_nobndry(*args, **kwargs):
    raise NotImplementedError(
        "Phase 2: mirror `fromFieldAligned(..., RGN_NOBNDRY)` is not landed yet."
    )
