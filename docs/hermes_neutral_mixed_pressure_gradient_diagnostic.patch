diff --git a/include/neutral_mixed.hxx b/include/neutral_mixed.hxx
index ca4f8b7c..7ec8b3ac 100644
--- a/include/neutral_mixed.hxx
+++ b/include/neutral_mixed.hxx
@@ -78,0 +79,5 @@ private:
+  Field3D momentum_parallel_inertia_source; ///< Parallel inertia momentum source diagnostic
+  Field3D momentum_pressure_gradient_source; ///< -Grad_par(Pn) momentum source diagnostic
+  Field3D momentum_perpendicular_diffusion_source; ///< Perpendicular diffusion momentum source diagnostic
+  Field3D momentum_parallel_viscosity_source; ///< Parallel viscosity momentum source diagnostic
+  Field3D momentum_perpendicular_viscosity_source; ///< Perpendicular viscosity momentum source diagnostic
diff --git a/src/neutral_mixed.cxx b/src/neutral_mixed.cxx
index 6f632c8f..de5ec14a 100644
--- a/src/neutral_mixed.cxx
+++ b/src/neutral_mixed.cxx
@@ -567,7 +567,7 @@ void NeutralMixed::finally(const Options& state) {
-    ddt(NVn) = -AA
-                   * FV::Div_par_fvv<ParLimiter>( // Momentum flow
-                       Nnlim, Vn, sound_speed)
-
-               - Grad_par(Pn); // Pressure gradient
-
+    momentum_parallel_inertia_source = -AA
+                                       * FV::Div_par_fvv<ParLimiter>( // Momentum flow
+                                           Nnlim, Vn, sound_speed);
+    momentum_pressure_gradient_source = -Grad_par(Pn);
+    ddt(NVn) = momentum_parallel_inertia_source + momentum_pressure_gradient_source;
+
     // Perpendicular advection of momentum
@@ -580,4 +581,7 @@ void NeutralMixed::finally(const Options& state) {
       ddt(NVn) +=
           Div_a_Grad_perp_flows(DnnNVn, logPnlim, mf_adv_perp_xlow, mf_adv_perp_ylow);
     }
-
+    momentum_perpendicular_diffusion_source =
+        ddt(NVn) - momentum_parallel_inertia_source - momentum_pressure_gradient_source;
+
     if (neutral_viscosity) {
@@ -591 +593 @@ void NeutralMixed::finally(const Options& state) {
-      Field3D viscosity_source = Div_par_K_Grad_par_mod( // Parallel viscosity
+      momentum_parallel_viscosity_source = Div_par_K_Grad_par_mod( // Parallel viscosity
@@ -594,0 +597 @@ void NeutralMixed::finally(const Options& state) {
+      Field3D viscosity_source = momentum_parallel_viscosity_source;
@@ -598 +601 @@ void NeutralMixed::finally(const Options& state) {
-        viscosity_source +=
+        momentum_perpendicular_viscosity_source =
@@ -601 +604 @@ void NeutralMixed::finally(const Options& state) {
-        viscosity_source +=
+        momentum_perpendicular_viscosity_source =
@@ -603,0 +607 @@ void NeutralMixed::finally(const Options& state) {
+      viscosity_source += momentum_perpendicular_viscosity_source;
@@ -773,0 +778,60 @@ void NeutralMixed::outputVars(Options& state) {
+    if (momentum_parallel_inertia_source.isAllocated()) {
+      set_with_attrs(
+          state[std::string("SNV") + name + std::string("_parallel_inertia")],
+          momentum_parallel_inertia_source,
+          {{"time_dimension", "t"},
+           {"units", "kg m^-2 s^-2"},
+           {"conversion", SI::Mp * Nnorm * Cs0 * Omega_ci},
+           {"standard_name", "momentum source"},
+           {"long_name", name + " parallel-inertia momentum source"},
+           {"species", name},
+           {"source", "neutral_mixed"}});
+    }
+    if (momentum_pressure_gradient_source.isAllocated()) {
+      set_with_attrs(
+          state[std::string("SNV") + name + std::string("_pressure_gradient")],
+          momentum_pressure_gradient_source,
+          {{"time_dimension", "t"},
+           {"units", "kg m^-2 s^-2"},
+           {"conversion", SI::Mp * Nnorm * Cs0 * Omega_ci},
+           {"standard_name", "momentum source"},
+           {"long_name", name + " pressure-gradient momentum source"},
+           {"species", name},
+           {"source", "neutral_mixed"}});
+    }
+    if (momentum_perpendicular_diffusion_source.isAllocated()) {
+      set_with_attrs(
+          state[std::string("SNV") + name + std::string("_perpendicular_diffusion")],
+          momentum_perpendicular_diffusion_source,
+          {{"time_dimension", "t"},
+           {"units", "kg m^-2 s^-2"},
+           {"conversion", SI::Mp * Nnorm * Cs0 * Omega_ci},
+           {"standard_name", "momentum source"},
+           {"long_name", name + " perpendicular-diffusion momentum source"},
+           {"species", name},
+           {"source", "neutral_mixed"}});
+    }
+    if (momentum_parallel_viscosity_source.isAllocated()) {
+      set_with_attrs(
+          state[std::string("SNV") + name + std::string("_parallel_viscosity")],
+          momentum_parallel_viscosity_source,
+          {{"time_dimension", "t"},
+           {"units", "kg m^-2 s^-2"},
+           {"conversion", SI::Mp * Nnorm * Cs0 * Omega_ci},
+           {"standard_name", "momentum source"},
+           {"long_name", name + " parallel-viscosity momentum source"},
+           {"species", name},
+           {"source", "neutral_mixed"}});
+    }
+    if (momentum_perpendicular_viscosity_source.isAllocated()) {
+      set_with_attrs(
+          state[std::string("SNV") + name + std::string("_perpendicular_viscosity")],
+          momentum_perpendicular_viscosity_source,
+          {{"time_dimension", "t"},
+           {"units", "kg m^-2 s^-2"},
+           {"conversion", SI::Mp * Nnorm * Cs0 * Omega_ci},
+           {"standard_name", "momentum source"},
+           {"long_name", name + " perpendicular-viscosity momentum source"},
+           {"species", name},
+           {"source", "neutral_mixed"}});
+    }
