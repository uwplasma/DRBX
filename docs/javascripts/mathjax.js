/* MathJax configuration for pymdownx.arithmatex (generic mode).
 * Loaded before the MathJax bundle listed in mkdocs.yml extra_javascript. */
window.MathJax = {
  tex: {
    inlineMath: [["\\(", "\\)"]],
    displayMath: [["\\[", "\\]"]],
    processEscapes: true,
    processEnvironments: true,
  },
  options: {
    ignoreHtmlClass: ".*|",
    processHtmlClass: "arithmatex",
  },
};
